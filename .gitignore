# Local Secrets & Scripts
# -----------------------

# Ignore the bulk add script (contains SERVICE_ROLE_KEY)
/app/add_users_function/bulkAddUsers.mjs

# Ignore all local environment files
.env.local
.env.development.local
.env.production.local
.env.test.local

# Supabase-specific env files
# (These are already handled by your supabase/.gitignore, 
# but adding them here is safe.)
.env.keys
.env.*.local
node_modules
